// basemaps
import Light from './basemaps/Light';

// layers
import Mapbooks from './layers/Mapbooks.js';

const LAYERS = {
    basemaps: {
        Light
    },
    layers: {
        Mapbooks
    }
}

export default LAYERS;
